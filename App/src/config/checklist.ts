// Lista padrão de itens do checklist de atividade (PRD 4.5) — [PENDENTE] no PRD: falta pedir pro
// Cristiano a lista exata que ele usa hoje. Até lá, usa os itens que ele mencionou verbalmente
// (checklist do veículo, amarração de carga). Fica num array central em vez de espalhado pela
// tela pra ser fácil de ajustar quando a lista oficial chegar — não precisa mexer em schema
// nem em query, só nesta lista.
export const ITENS_CHECKLIST_ATIVIDADE = [
  'Checklist do veículo (pneus, freios, luzes, óleo)',
  'Amarração de carga',
  'Cinto de segurança',
  'Documentação do veículo em dia',
] as const;
