import { useCallback, useEffect, useState } from 'react';
import { RefreshCw, Wrench } from 'lucide-react';
import { useAuth } from '../../context/AuthContext'; import { cabecalhoPerfil } from '../../utils/apiAuth';
const API = import.meta.env.VITE_API_URL ?? '';
export function Manutencao() { const { usuario } = useAuth(); const [d,setD]=useState<any>(null); const [e,setE]=useState(''); const [f,setF]=useState({nome:'',tipo:'',codigoEquipamento:'',localizacao:''}); const load=useCallback(async()=>{try{const r=await fetch(`${API}/api/manutencao`,{headers:cabecalhoPerfil(usuario?.perfil)});const b=await r.json();if(!r.ok)throw new Error(b.error);setD(b);setE('')}catch(x){setE(x instanceof Error?x.message:'Falha ao carregar manutenção.')}},[usuario?.perfil]);useEffect(()=>{load()},[load]);const salvar=async(x:React.FormEvent)=>{x.preventDefault();const r=await fetch(`${API}/api/manutencao`,{method:'POST',headers:{...cabecalhoPerfil(usuario?.perfil),'Content-Type':'application/json'},body:JSON.stringify({acao:'ativo',...f})});const b=await r.json();if(!r.ok)return setE(b.error);setF({nome:'',tipo:'',codigoEquipamento:'',localizacao:''});load()};const k=d?.kpis??{};return <div className="space-y-6 max-w-7xl"><div className="flex justify-between"><div><p className="text-xs font-bold uppercase text-emerald-700">Manutenção</p><h1 className="text-2xl font-bold">Painel de Manutenção</h1></div><button onClick={load} className="bg-emerald-600 text-white px-4 rounded-xl"><RefreshCw size={16}/></button></div>{e&&<p className="p-3 bg-rose-50 text-rose-700 rounded-xl">{e}</p>}<div className="grid grid-cols-2 lg:grid-cols-4 gap-4">{[['OS abertas',k.OSABERTAS],['OS atrasadas',k.OSATRASADAS],['Custo 30 dias',`R$ ${Number(k.CUSTO30DIAS??0).toLocaleString('pt-BR')}`],['Preventivas',k.PREVENTIVAS]].map(([a,b])=><div className="bg-white border-slate-200 border rounded-2xl p-5"><p className="text-xs text-slate-500">{a}</p><b className="text-2xl">{String(b??0)}</b></div>)}</div><section className="grid lg:grid-cols-3 gap-5"><form onSubmit={salvar} className="bg-white border-slate-200 border rounded-2xl p-5 space-y-3"><h2 className="font-bold flex gap-2"><Wrench/>Cadastrar ativo</h2>{['nome','tipo','codigoEquipamento','localizacao'].map(c=><input required={c==='nome'||c==='tipo'} placeholder={c==='nome'?'Nome do ativo':c==='tipo'?'Tipo':c==='codigoEquipamento'?'Código do equipamento':'Localização'} value={(f as any)[c]} onChange={x=>setF({...f,[c]:x.target.value})} className="w-full border border-slate-200 rounded-xl p-2"/>)}<button className="bg-emerald-600 text-white px-4 py-2 rounded-xl">Salvar ativo</button></form><section className="lg:col-span-2 bg-white border-slate-200 border rounded-2xl p-5"><h2 className="font-bold">Ordens de serviço</h2><div className="mt-3 space-y-2">{(d?.ordens??[]).map((o:any)=><div className="border-b py-2"><b>{o.AtivoNome}</b> · {o.Descricao} <span className="float-right">{o.Status}</span></div>)}{!d?.ordens?.length&&<p className="text-slate-400">Nenhuma OS cadastrada.</p>}</div></section></section></div> }
